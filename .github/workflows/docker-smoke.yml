name: docker-smoke

on:
  push:
  pull_request:

jobs:
  docker-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: docker build -t tcl-stakeholder .
      - run: docker run --rm tcl-stakeholder --list-values --output-format json
